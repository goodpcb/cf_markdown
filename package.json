{
  "name": "markdown-worker",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "marked": "^12.0.0",
    "wrangler": "^4.0.0"
  },
  "scripts": {
    "deploy": "wrangler deploy"
  }
}
